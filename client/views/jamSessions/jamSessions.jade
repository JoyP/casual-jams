.row
  .col-xs-4
  .col-xs-4
    h3 Sessions
  .col-xs-4

.row
  .col-xs-8
  .col-xs-4
    button.btn.btn-danger.sessions(ng-click='showFind()') Find Session
    button.btn.btn-danger.sessions(ng-click='showMySessions()') My Sessions
    button.btn.btn-danger.sessions(ng-click='showNew()') New Session

.row(ng-show='new')
  .col-xs-3
    form(role='form', novalidate, name='newForm')
      .form-group
        h4 Create New Session
      .form-group
        label(for='name') Session Name
        input.form-control#name(type='text', name='name', ng-model='newSession.name', autofocus, required)
      .form-group
        label(for='style') Session Style
        input.form-control#style(type='text', name='style', ng-model='newSession.style')
      .form-group
        label(for='loc') Location
        input.form-control#location(type='text', name='loc', ng-model='newSession.loc.city')
      .form-group
        label(for='date') Date
        input.form-control#date(type='date', name='date', ng-model='newSession.date')
      .form-group
        label(for='time') Time
        input.form-control#time(type='time', name='time', ng-model='newSession.time')
      button.btn.btn-danger(ng-show='newForm.$valid', ng-click='addNew()') Create
  .col-xs-9

.row(ng-show='show')
  .col-xs-8
    .panel
      .panel-body#show
        .row
          .col-xs-6
            h4 {{jamSession.name}}
            h5 Style: {{jamSession.style}}
            h5 {{jamSession.date | date:fullDate}}
            h5 {{jamSession.time | date: 'h:mma'}}
            h5 {{jamSession.loc.city}}
          .col-xs-6
            h4 Hosted by:
            h5 jamSession.hostId-name
        .row
          .col-xs-6
            h4 Attending:
            h5 ng-repeat members
          .col-xs-6
            button.btn.btn-danger Join Session!
  .col-xs-4

.row(ng-show='find')
  .col-xs-12
    h4 Find Sessions
    table.table.table-bordered
      thead
        tr
          th Name
          th Style
          th Date
          th Time
          th Location
      tbody#findSessions
        tr(ng-repeat= 'jamSession in findSessions', ng-click= 'showSession(jamSession)', data-id= '{{jamSession._id}}')
          td {{jamSession.name}}
          td {{jamSession.style}}
          td {{jamSession.date | date:fullDate}}
          td {{jamSession.time | date:'h:mma'}}
          td {{jamSession.loc.city}}

.row(ng-show='my')
  .col-xs-12
    h4 My Sessions
    table.table.table-bordered.sessions
      thead
        tr
          th Name
          th Style
          th Date
          th Time
          th Location
      tbody
        tr
          td jamSession.name
          td jamSession.style
          td jamSession.date
          td jamSession.time
          td jamSession.loc.city

